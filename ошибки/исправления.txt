Task 1 якщо в проекті більше одного css файла, створюй для них окрему папку (-2) //Испаравил
сортування по значеню не завжди відпрацьовує коректно, якщо ввести a=2 і b=10, то виведе b, a (-5) //исправил
дивне створення масиву [{}]; // Изменил
splice() виглядає як логічний мусор //счет идет с 1



Task 2 пакуй все по структурі. Файли які відносяться до юзера в окрему папку,
до поста в окрему до коментарів в окрему; css тоже розділяй на окремі файли (-3) //разделил по папкам html, css, js

не коректний query параметр userPost.id=, правильно буде postId=5&userId=1.
Твій варіант працює, але є абсолютно неправильним підходом (-3) //исправил

у post-details ти отримуєш пост не по id, ти получаєш всі пости юзера, а потім циклом шукаєш той,
що співпадає з postId (-3)  // исправил

додаєш boxPost у body всередині fetch → дублювання DOM, якщо код викличеться 2 рази,
ти отримаєш 100500 блокі постів (наприклад натиснувши декілька разів кнопку постів юзера) (-3) //исправил использую флаг

створюєш зайві змінні, які ніде не використовуються, також зайві await, тому що ти працюєш з than (-1)
краще не будувати структуру через innerHTML"  // исправил
